{
	"client.toml": {
		"keyring-backend": "{{ client_keyring_backend }}",
		"broadcast-mode": "{{ client_broadcast_mode }}"
	},
	"app.toml": {
		"minimum_gas_prices": "{{ minimum_gas_prices }}",
		"pruning": "{{ pruning }}",
		"pruning-keep-recent": "{{ pruning_keep_recent }}",
		"pruning-keep-every": "{{ pruning_keep_every }}",
		"pruning-interval": "{{ pruning_interval }}",
		"halt-height": "{{ halt_height }}",
		"halt-time": "{{ halt_time }}",
		"min-retain-blocks": "{{ min_retain_blocks }}",
		"inter-block-cache": "{{ inter_block_cache }}",
		"telemetry.service-name": "{{ telemetry_service_name }}",
		"telemetry.enabled": "{{ telemetry_enabled }}",
		"telemetry.enable-hostname": "{{ telemetry_enable_hostname }}",
		"telemetry.enable-hostname-label": "{{ telemetry_enable_hostname_label }}",
		"telemetry.enable-service-label": "{{ telemetry_enable_service_label }}",
		"telemetry.prometius-retention-time": "{{ prometius_retention_time }}",
		"api.enable": "{{ api_enabled }}",
		"api.swagger": "{{ api_swagger }}",
		"api.adddress": "tcp://0.0.0.0:{{ api_port }}",
		"grpc.enable": "{{ grpc_enabled }}",
		"grpc.address": "tcp://0.0.0.0:{{ grpc_port }}",
		"state-sync.snapshot-interval": "{{ statesync_snapshot_interval }}",
		"state-sync.snapshot-keep-recent": "{{ statesync_snapshot_keep_recent }}"
	},
	"config.toml": {
		"fast_sync": "{{ fast_sync }}",
		"abci": "{{ abci }}",
		"rpc.laddr": "tcp://0.0.0.0:{{ rpc_port }}",
		"p2p.laddr": "tcp://0.0.0.0:{{ p2p_port }}",
		"p2p.external_address": "{{ p2p_external_address }}",
		"p2p.seeds": "{{ p2p_seeds }}",
		"p2p.persistent_peers": "{{ p2p_persistent_peers }}",
		"p2p.upnp": "{{ p2p_upnp }}",
		"p2p.pex": "{{ p2p_pex }}",
		"p2p.seed_mode": "{{ p2p_seed_mode }}",
		"p2p.private_peer_ids": "{{ p2p_private_peer_ids }}",
		"statesync.enable": "{{ statesync_enabled }}",
		"statesync.rpc_servers": "{{ statesync_rpc_servers }}",
		"statesync.trust_height": "{{ statesync_trust_height }}",
		"statesync.trust_hash": "{{ statesync_trust_hash }}",
		"statesync.trust_period": "{{ statesync_trust_period }}",
		"statesync.discovery_time": "{{ statesync_discovery_time }}",
		"statesync.chunk_request_timeout": "{{ statesync_chunk_request_timeout }}",
		"statesync.chunk_fetchers": "{{ statesync_chunk_fetchers }}",
		"fastsync.version": "{{ fastsync_version }}",
		"instrumentation.prometheus": "{{ prometheus_enabled }}",
		"instrumentation.prometheus_listen_addr": "{{ prometheus_listen_addr }}",
		"instrumentation.namespace": "{{ instrumentation_namespace }}"
	}
}
